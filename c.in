
struct bst {
	int t;
	int u;
	int v;
};

int func(int x) {
	int a = 2 + 4 * ( (6 + 5 ) * 2), b = 5;
	int c = a;
	
	b = !a + 2;

	while (a < 10)
	if (b == 6)
		break;
	done: ++a;

	if (a == 5)
	if (b < 4)
		if (!c)
			a = a + 2;
	else
		a = 10;

	b = c;

	goto done;
	return (c + a) * 2;
}



int nujh() {
	int a = 2 + 4 * ( (6 + 5 ) * 2), b = 5;
	int c;
	struct bst b2;
	b2.v = 4;
	b = b2.v;
	b = !a + 2;

	while (a < 10)
	if (b == 6)
		break;
	done: ++a;

	if (a == 5)
	if (b < 4)
		if (!c)
			a = a + 2;
	else
		a = 10;

	b = c;
	c = func(b);

	goto done;
}
